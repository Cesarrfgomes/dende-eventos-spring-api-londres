services:
  db:
    image: mysql:8.4.0
    container_name: dende_eventos
    restart: always
    environment:
      MYSQL_DATABASE: dende_eventos
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    ports:
      - "3306:3306"